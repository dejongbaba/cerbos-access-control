apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "document"
  rules:
    - actions:
        - edit
      effect: EFFECT_ALLOW
      roles:
        - admin
      condition:
        match:
          expr: "time.now().hour >= 20"
    - actions:
        - view
      effect: EFFECT_ALLOW
      roles:
        - admin
